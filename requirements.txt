numpy>=1.24
fastapi>=0.110
uvicorn[standard]>=0.24
python-dotenv>=1.0
loguru>=0.7
requests>=2.31
beautifulsoup4>=4.12
regex>=2023.12.25
streamlit>=1.39.0

# Embeddings (choose one or both)
sentence-transformers>=2.7
fastembed>=0.3.5

# Vector stores (NumPy-only runtime)

# LLM (Gemini)
google-generativeai>=0.7.2

# Optional utilities (not strictly required)
tqdm>=4.64.0
lxml>=4.9.0
aiohttp>=3.8.0
trafilatura>=1.6.0
readability-lxml>=0.8.0
pytest>=7.0.0
pytest-asyncio>=0.21.0
